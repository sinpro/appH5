export default {

    // 魔方配置信息
    data: {
        type: Object,
        default () {
            return {}
        }
    },

    // 图片间距
    itemMargin: {
        type: String,
        default: '0'
    },

    // 上下间距
    pageTopBottomMargin: {
        type: String,
        default: '0',
    },

    // 左右间距
    pageLeftRightMargin: {
        type: String,
        default: '0',
    },
}